services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
      PORT: 3000

volumes:
  node_modules:
